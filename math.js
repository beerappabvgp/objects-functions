console.log(Math);

// abs - absolute

let num = -900;

console.log(Math.abs(num));

let num1 = 5.9; // smallest nearest integer 
console.log(Math.floor(num1));  // 5
console.log(Math.round(num1)); // 0 to 0.5 => it gets smallest nearest integer
// If not the case it gets the largest nearest integer 
console.log(Math.ceil(8.9)); // largest nearest integer 

console.log(Math.pow(2,3));